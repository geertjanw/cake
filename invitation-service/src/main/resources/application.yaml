spring:
  application:
    name: invitation-service

server:
  port: 8082

logging:
  level:
    com.cakeandcandles: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info
